{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install -r sotopia-chat/requirements.txt"
  },
  "deploy": {
    "startCommand": "python -m uvicorn sotopia-chat.fastapi_server:app --host 0.0.0.0 --port $PORT"
  }
}
